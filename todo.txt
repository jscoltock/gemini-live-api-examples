clean up agent descriptions
agent response is getting truncated
install playwright for everyone
put agent stuff for all llms on right pane



add upload images and or files TEST
128k model for qwen and gemma
make qwen fallback for gemma and vice versa and glm fallback for both\

multi-agent? gemini image emailed etc
test chron jobs


simplify skill?  
code review tool on this codebase
add home automation assistant - will a local model work?  Need new tool
in agent configuation only show Ollama Tools if the Backend is Ollama - dont show if its claude code
make all prompts optimized to use less token by being more concise
button to show recent output of logs
have system determine if a local model can do a given task before giving it to a cloud model
show agent progress so I dont think its hung
deal with concurrency limits on z.ai models
add agent that is second brain - infinite memory karpathy or obsidian?


x give local all tools claude code has - twilio and tavily
x have chat pane have full screen toggle
x telegram
x show the agent thinking somewhere for complete observability
x keep ollama model in memory
x the ollama configuration should be able to handle many tools not just the four we started with
x clean up project - no need for command line or ephemeral folders
x give gemma4 more context.
x clear agent display
x accumulate the costs
x install context7
X use smaller gemini live model (dont bother - older models cost the same amount)
X add model and timeout to right pane 
X add backend harness to right pane
X add fallback configurations (all parms -api key model)
X add gemini api key fallback
X add util to edit agents instead of manually editing yaml file
x show google-live api costs
X allow editing system prompt for gemini
X make sure right pane shows correct model and harness (in case fallback is being used)
X improve UI to show more info (no space between header and rest of stufff,
                              smaller font in chat (same as right pane,
                              make type a message box same width as chat pane,
                              move panes around))
x change the system. prompt so gemini is the Chief of Staff and can do things related to vision and sound
x ollama tool calling make ollama to what claude code does
X can local models use tools like bash, read, edit, write 
x make a read me file for the current project
x main agent must say exactly what. the subagent said not summarize
x Don't call gemini at all if not needed (no voice, VAD, images etc)